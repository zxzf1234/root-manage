# 本地开发环境
NODE_ENV=development

VITE_DEV=true

VITE_SERVER_IP='localhost'

VITE_SERVER_PORT='48080'

# 后端请求路径
VITE_BASE_PATH=/root-client/

# 默认账号密码
VITE_DEFAULT_USERNAME=admin
VITE_DEFAULT_PASSWORD=admin123

VITE_OUT_DIR=dist-base

# 服务器环境
VITE_SERVER_ENV=local
VITE_DEFAULT_ACCOUNT_NAME=root试用